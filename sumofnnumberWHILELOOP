#include<iostream>
using namespace std;
int main(){
	int i=0,n,m,sum=0;
	cout << "Enter a number:" ;
	cin >> n;
	while(i<n){
		cout << "Enter number: ";
		cin >> m;
		sum += m;
		i++;
	}
	cout << "Sum of " << n << " numbers is " << sum;
	return 0;
}
