#include <iostream>
int main() {
    int arr[] = {10, 20, 30, 40, 50};
    for (int i = 0; i < 5; ++i) {
        std::cout << "Address of arr[" << i << "]: " << &arr[i] << std::endl;
    }

    return 0;
}
