#include<iostream>
using namespace std;
int main()
{
	int i=1,n,sum=0;
	cout<<"enter the number:";
	cin>>n;
	while(i<n)
	{
		if(n%i==0)
			sum+=i;
			i++;
	}
	if(sum==n)
	{
		cout<<"is a perfect";
	}
	else{
		cout<<"is not a perfect number";
	}
}
