#include <iostream>
int main() {
    int num;
    std::cout << "Enter an integer: ";
    std::cin >> num;
    std::cout << "Value of the variable: " << num << std::endl;
    std::cout << "Address of the variable: " << &num << std::endl;

    return 0;
}
