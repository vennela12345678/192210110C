#include<iostream>
#include<cmath>
using namespace std;
int main()
{
	int n,sum=0,i;
	cout<<"enter the number:";
	cin>>n;
	for(i=1;i<n;i++){
			if(n%i==0)
				sum+=i;
	}
	if(sum==n)
	{
		cout<<"is a perfect number";
	}
	else
	{
		cout<<"is not a perfect number";
	}
	return 0;
}
